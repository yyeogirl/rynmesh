# 阅读与偏好导出文件核对

2026-09-14，Windows / Chrome，当前安装候选 `1829eb20296cb3d5ceb23e6409d57ae686a3e6a9`，节点 18990。补 READ10 中“浏览器下载文件落盘”的证据，复用已完成的导出失败、重试和重置恢复结果。

1. 实际页面 Settings → Privacy & data → Export reading & preferences (JSON)。Chrome 在下载目录生成完整 JSON 文件，12535 字节，解析成功。
2. 只读获取同节点导出接口，对照文件内画像、偏好、两条阅读历史、来源、首次成功与隐私设置，六组字段逐项一致。导出时间不要求与后来请求一致，后台审计也不作为静态字段比较。
3. 复查此前同日实际下载的 18622 字节文件：四条反馈事件 ID 与首次阅读隐私验收检查点完全一致；整个画像的 SHA-256 摘要也一致。该旧文件中的解释字段因而可复用原接口核对结果，无需重造反馈并再次重置。

校验摘要见 [JSON 记录](reading-export-file-20260914.json)。文件正文、来源 URL 和个人路径不复制进仓库。检查的是实际浏览器保存的文件，不仅是页面成功提示或 HTTP 响应。

本结果仅关闭导出文件证据缺口。READ10 的完整日志与网络边界仍须按原要求核对；不因导出成功直接宣称全部隐私验收通过。macOS 和公网按用户要求跳过。
