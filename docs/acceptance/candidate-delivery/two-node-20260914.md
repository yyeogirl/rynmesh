# 已安装候选的双节点流程

代码 `1829eb20296cb3d5ceb23e6409d57ae686a3e6a9`；Windows / Python 3.12 / Chrome。两个独立节点从同一已安装 wheel 运行，分别监听 18990、18991，数据、身份和密钥分开。不使用源码开发服务器，不预置配对、消息或卡片。A 沿用上一轮页面保存的公开文章，B 新建空数据目录。

本轮修复试用启动配置遗漏的设备回环配对选项，并提供第二节点独立启动/停止入口。仅改变本机试用启动配置，未放宽产品默认端点检查。

实际页面结果：

1. A 仅选择收藏生成设备邀请；B 本地审核身份，只选择收藏并请求配对；两端核对同一代码，A 再次勾选范围并批准。
2. 两端显示配对确认、收藏范围、零条待确认。B 在 My reading 的 Saved content 中看到 A 的收藏，显示尚未在本机打开。未开启阅读进度或会话同步。
3. A 创建好友邀请；B 粘贴安装说明和邀请，审核身份、地址与权限，再明确接受。设备配对与好友配对各自独立执行。
4. B 页面发送第一条合成消息，显示由对方确认送达。
5. A 从已有收藏打开正文并分享卡片；B 先看到元数据与主动下载入口，随后点击保存并读取正文。双方独立副本均为 3403 字节，SHA-256 相同，见 JSON 记录。
6. A 在说明旧副本无法收回的确认框中撤销好友。两端关系均为 revoked；B 关闭后重新打开保存副本，仍能读取。设备配对继续 active，所选收藏变化均已确认。

原始状态摘要见 [two-node-result.json](two-node-result.json)。不保存邀请、关系秘密或正文。

本轮结束时两个节点保留运行供用户试用。好友关系已撤销，若需继续互发内容，应通过新邀请重新配对；设备配对仅允许收藏。A 原数据目录保留，B 使用独立目录。

范围限制：这是 Windows 上已安装 Python wheel 与浏览器的双节点回环验证，仍不是 macOS 桌面安装、两个真实设备、公网或远端 CI。没有重新验证附件、邮箱离线、全部同步范围、冲突及其他既有路径，不改记全部验收通过。
