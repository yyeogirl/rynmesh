# 多设备同步开发记录

最新续验（2026-09-12）：阅读清理后的万条 v4/v3 同步，首次传输错误后能从剩余 5000 条确认恢复。100 次重新收藏与确认原测量 61.130 秒超标；减少未变化删除凭据的重复验证后复测 56.831 秒。数据及全部增量确认检查通过，失败与复测证据均保留。见 [v4 规模记录](source-v4-scale.md)；完整数据维护、打包桌面和跨网验收仍未完成。

状态：收藏/阅读及会话来源、Ask Ryn 分支恢复、“我的设备”双端配对、范围控制与加密批次传输已接入开发分支，并显示来源确认进度；阅读冲突选择、完整擦除、规模性能及打包跨网验收仍未完成，不是产品验收通过声明。

## 本轮实现

新增 `rynmesh/device_sync/records.py` 与 `store.py`：

- 收藏、阅读进度和会话使用每个内容实体的设备计数器与并发值。合并不依赖机器时间，重复和乱序快照可收敛。
- 收藏并发新增/取消时取消优先，但保留冲突来源；看到合并结果后明确再次收藏可以恢复。删除的操作记录不会随旧快照回来而丢失。
- 阅读进度按操作的因果顺序处理，向前重读也有效；并发位置保留多个候选，不使用最大百分比代替用户选择。材料版本标识可随位置记录。
- 会话保留原 Provider、服务键、网络和创建身份。并发续聊形成分支，提供共同消息前缀；删除与新增并发时，新增分支进入恢复项。明确擦除会话会删除恢复正文并保留阻止旧投递复活的标记。
- 收藏和阅读只允许明确的元数据字段，正文、摘要、图片地址和任意扩展不进入同步值。会话仅保留已终结消息与材料引用，排除草稿、凭证、运行中消息及其关联请求。
- 存储复用节点消息密钥、现有加密模块、进程共享文件锁和原子写入，使用独立加密通道。文件位于节点 `device-sync/replica.json`，未知版本拒绝修改，本地未知扩展字段保留且不进入发送批次。
- 调用者必须明确指定同步范围；没有范围时不产生发送批次。批次最多 100 条、12 MiB；整个批次存储成功后才返回确认。过期确认不会把较新的本地记录标成已同步，重新配对前可以清除旧确认。

当前上限为 64 个设备标识、合计 3 万个实体（含删除记录）、单实体 8 MiB、总明文 128 MiB。达到限制会拒绝本次操作并保留旧数据，不自动清除删除记录或冲突正文。这些上限还需要在后续产品页面中明确展示。

## 证据与限制

环境：Windows，本地 Python 虚拟环境，真实临时目录与加密密钥；全为合成数据，无外部请求。

- 合并测试覆盖并发取消/再收藏、向前重读、并发位置选择、三台逻辑设备的重复乱序组合、会话共同历史与分支、删除恢复项、擦除后旧快照拒绝、同一操作身份的冲突正文拒绝，以及元数据范围限制。
- 存储测试覆盖选择范围、接收成功但确认尚未返回、过期确认、写入前失败、写入已提交但响应丢失后的重启重试、八个实例竞争同一修订、未来/损坏文件保留、本地扩展保留与发送字段过滤。
- 两份真实加密存储预置各 1 万条已经相互确认的记录后，新增 100 条，按 `write_many → pending → receive → acknowledge → pending=0` 计时。第一次 3.636 秒，追加确认校验后的复测 2.864 秒。初始基线由测试直接构造；不包含初始合并、来源适配、设备握手、HTTP 传输或页面渲染，不能据此宣称 SYNC04 已通过。
- 第一轮新模块检查 26 passed，8.19 秒；随后与现有 Ask 材料、阅读记录的相关回归 46 passed / 1 warning，12.11 秒，Ruff 通过。警告为既存的 TestClient 弃用提示。之后补充同一操作的数值编码变化保护：不能因 Python 把 `0` 与 `0.0` 判成相等，让不同顺序合并得到不同哈希；最终新模块专项 27 passed，6.81 秒，Ruff 通过。

## 接入必须满足的约束

1. 本地收藏、取消、进度和会话修改，需要在同一次可靠写入中保存对应的同步操作身份和所见版本。不能仅定时比较最新值，否则中间操作及因果关系会丢失。原存储升级需备份，来源清理和列表淘汰不能误删同步删除记录。
2. 接收端把变更落到真实收藏、阅读和会话来源后，才能向另一设备确认产品层完成。当前 `ReplicaStore.receive` 的确认仅证明副本存储已持久化，接入层不得提前把它当作页面数据已更新。
3. 设备配对必须独立于好友关系，两端确认身份、邀请时效/一次性、选定范围、加密认证完成后才允许传个人数据。当前内部方法没有提供此授权，也不能直接作为公开 HTTP 端点。
4. 配对移除、暂停、范围关闭及重新加入需要实际状态机；`pending` 的范围过滤和确认清理只是所需组件，不能替代身份撤销。
5. 冲突选择需要核对用户已审阅的修订；会话恢复需明确创建新分支身份，不恢复旧删除身份。不复制运行中任务，也不得自动换服务重发历史。

## 收藏与阅读来源适配

新增 `reading.py`、`reading_bridge.py`，并接入 `ConsumptionStore` 的实际读写：

- 内部显式启用后，收藏/取消和阅读位置在同一次原子写入中保存操作身份。尚未提供用户启用入口；普通安装保持原来的本地格式，不自动开始同步。
- 旧记录升级前完整备份，保留本地扩展字段；未来版本拒绝修改。备份采用阅读存储自己的 96 MiB 上限，已覆盖超过通用 16 MiB 限制的旧文件。新增信封最多 256 MiB、收藏与阅读共 2 万个实体（含删除记录），容量不足时保留原文件。
- 最近历史的淘汰不会删除同步操作或仍在使用的收藏/进度；清空会保存因果删除标记，旧快照不能恢复已经观察并删除的数据。这里只处理阅读/收藏，不能代替 G08 的全应用擦除。
- 普通阅读保留未处理的其他设备位置；明确选择须携带审阅过的修订。第三台设备首次收到冲突时只显示元数据和冲突状态，不任选一个候选位置。候选查询已在内部来源接口中提供，页面选择入口待接入。
- `ReadingBridge` 按“来源保存 → 加密副本保存 → 返回确认”处理接收。两步之间失败不返回确认，来源中已提交内容仍可读，重启后可重复投递。发送准备及确认处理前会补齐本机来源，防止过期确认把后来发生的本地修改标成已同步。
- 副本损坏不会阻止正常本地阅读写入；修复副本后可从来源补齐。构造适配器不会授权设备，不会启用范围；来源身份必须与节点副本身份一致。来源与副本采用统一的锁顺序，网络调用不在锁内执行。
- 现有本地阅读 API 可携带内容版本和审阅修订；过期修订返回 409，存储不可用返回 503。保存操作放到工作线程执行，等待存储时健康检查仍可响应。未增加可绕过配对的远程同步路由。

新增真实临时文件测试覆盖迁移/备份失败、历史淘汰、写入前失败、提交后响应丢失、重启重投、并发收藏与位置、第三端冲突、明确选择过期、向前重读、清空后旧投递、元数据过滤、八个来源实例并发、来源/副本分别失败、过期确认和锁顺序。没有外部网络、真实双桌面或 GPU 测试。

验证结果：来源与桥接最初专项 53 项（修正一条断言：仅同步收藏的接收端取消后可直接从列表消失），最终通过。随后后端全量 `pytest tests/ -q` 为 **1158 passed / 29 skipped / 5 warnings，213.82 秒**。在此后补充大文件备份、备份失败、HTTP 修订冲突与非阻塞保存检查，最终六个相关测试模块 **85 passed / 1 warning，14.94 秒**，涉及修改文件的 Ruff 通过。全量结果不包含后补的四个用例；后补变更以这次专项回归为证据。警告均为现有 TestClient/FastAPI 弃用提示，跳过项不算验收通过。

## 会话来源、运行中任务与恢复入口

新增 `conversations.py` 与 `conversation_bridge.py`，在 `ConversationStore._write` 统一捕获来源变化，因此后台 `AskRunService` 归档结果也与同步操作一起提交。

- 显式启用时备份原加密文件并升级到 v2；不会自动启用或传输。来源记录、候选分支、恢复映射继续加密，保留未知本地扩展且不发送这些扩展。普通历史仍采用原来的容量预算；启用同步后为 64 MiB 明文、88 MiB 加密信封，最多 1000 个会话和 10000 个因果实体（含删除记录），达到限制拒绝操作并保留旧文件。
- 正常写入沿本机所见分支继续；远端并发分支单独保留。第三台逻辑设备仅收到冲突时不会任意挑一条放进普通历史，候选在专用列表中完整可读。恢复必须审核修订，并创建新会话身份，保留原 Provider/服务键/网络。
- 运行中修改、问题及自动标题不会提前进入同步。初次启用发生在运行中时，也会过滤未完成输入与自动标题。远端更新不会替换正在使用的上下文；本地结果完成后保留并发分支。
- 远端普通删除会隐藏原会话并请求取消原任务，避免新请求继续使用已删除身份；原任务仍按原 ID 对账，不重发。已经发生的迟到结果进入恢复项，不回到主历史。明确的因果擦除则不保留迟到正文。
- 远端删除时，本机未发送草稿留在本机恢复区，并保留原服务绑定。它不进入同步批次；用户可单独保留为新会话。个人导出在一个来源锁内取得普通历史、草稿和恢复项的一致快照。
- 桥接层依然先保存来源、再保存副本、最后确认；源归档或副本写入失败不会确认，重试可恢复。旧确认不会覆盖后来完成的本地回答。
- Ask Ryn 增加分支与恢复区：分别展开完整正文、显示原接收者、保留为独立会话，无模型也能阅读。恢复使用审核版本和稳定的新身份，刷新/响应丢失后重试不会重复新增；恢复后焦点和页面定位到新历史。聊天页在出现其他设备变更时提供复核入口，删除说明同步范围及并发恢复项。

安全边界：没有开放远程同步写入或自动配对。本地历史/恢复 API 继续受 owner 控制，恢复不会调用模型。当前内部 `sync_erase` 只实现来源的因果擦除；**尚未完成迁移备份、旧副本、浏览器恢复副本和订单结果的统一擦除与远端确认**，因此未提供产品“全部擦除成功”按钮，G08/SYNC13 仍未通过。保留分支后原冲突仍可复核，关闭/丢弃恢复项的完整产品流程待补。

验证：新增会话来源专项最初 17 项，与历史、任务、上下文及副本的相关回归 **80 passed / 1 warning，12.12 秒**；随后后端全量 **1179 passed / 29 skipped / 5 warnings，217.24 秒**。前端全量最初 **137 passed / 30 files，21.99 秒**；浏览器检查后补充恢复定位与本机草稿保护，后补验证结果见下文。类型检查、构建、Ruff 通过；构建仍提示既有主 JS 包超过 500 kB。

浏览器证据：用 `scripts/accept_conversation_sync_node.py` 在独立 `rynmesh-conversation-sync-acceptance-a` 目录启动 `127.0.0.1:18872`，真实的两个加密来源通过内部 API 创建合成分支和删除冲突，关闭模型与公开发现。浏览器实际完成“展开删除恢复分支 → 保留独立会话 → 阅读原回复”，显示原服务不可用但历史可读；刷新后再次保留返回同一个 `restored_…` 身份，普通列表仍只有原活跃会话和新保留会话。停止节点进程后以 `--reuse` 重启，再次打开相同 URL，正文、原服务键和选中焦点保留。检查使用 1280 像素浏览器视口，DOM 未发现横向内容溢出；没有据此推断其他窗口宽度的验收结果。该证据不包含设备配对、实际跨设备传输、GPU 或打包桌面。

最终后补验证：会话来源/历史/任务三个模块 **39 passed / 1 warning，16.31 秒**，包含远端删除保留本机草稿、恢复不转移草稿、显式源擦除移除草稿恢复项、未知本地凭证不进入个人导出。前端两个相关模块先 **10 passed，4.56 秒**，再全量 **139 passed / 30 files，27.95 秒**；TypeScript、Ruff 和生产构建通过，最终主 JS 为 `index-S377tH26.js`（575.07 kB）。最后的本机草稿保护在自动化测试中验证，未重新进行该路径的浏览器操作；后端 1179 项全量结果早于这项后补变更。临时验收标签和节点均已关闭，隔离数据目录保留供复现。

该阶段待完成的配对和范围控制已有下述进展；个人内容传输、同步状态、阅读位置选择、恢复项关闭/丢弃、完整擦除及发布平台验收仍待完成。保留副本被用户再次删除后，当前稳定恢复身份会拒绝复活它；还需提供明确创建另一份恢复副本的新身份流程。需求 01–07 的剩余通用验收也保留。

## 双端配对、范围控制与移除

新增 `pair_crypto.py`、`pair_store.py`、`pairing.py`、`routes.py`，页面入口为 Settings → My devices（`/devices`）。

- 邀请签名绑定节点身份、消息密钥、范围与有效期；默认 15 分钟，一次只能绑定一个加入请求。新电脑先审核邀请，原电脑再审核请求，两端核对相同代码。接收请求、批准、收到能力秘密、确认回执分别持久化，不能把收到请求当成配对成功。
- 加入与确认请求先写入加密容器再发送。回执丢失、进程重启、邀请过期后已提交的最终确认重试不会重复配对。改名不改变密钥身份；同一电脑或已活跃设备不能重复配对。过期、取消、篡改邀请与错误接收者均拒绝。
- 三类范围默认不勾选，两端有效范围取交集。暂停、范围变化采用各自递增修订；恢复后旧修订不能授权数据写入，不依赖两台电脑时钟一致。授权上下文持有配对锁直至本地来源提交，移除和范围收缩与该提交串行；网络调用不持有此锁。
- 移除先在本机保存拒绝状态、清除能力秘密，再由共享后台任务送出签名加密通知。对端提交后才确认送达；回应丢失可重试，重新配对使用新的配对身份，旧移除通知不会删除新配对。页面明确说明已复制内容无法收回。
- 复用现有 Ed25519、消息密钥和加密传输原语；配对、策略、移除与磁盘容器使用不同通道。磁盘只保存加密信封，公开状态不含秘密或完整握手报文，未知本地扩展保留。未来版本、坏密文或无效策略拒绝修改并保留原文件。
- 容器目前上限为各 256 条邀请/配对、8 MiB 明文、12 MiB 加密文件；无自动清理。网络报文最多 64 KiB，使用受限 IP 地址、5 秒超时和有界响应；每来源每分钟最多 60 次控制请求，来源缓存最多 256 项。尚未提供地址变更、旧邀请清理和容量管理的完整产品流程。
- 新设备缺少可达地址时返回可解释状态，不阻止整个应用启动或本机移除已有设备。开发环境可显式使用 `RYNMESH_DEVICE_ENDPOINT` 与 `RYNMESH_DEVICE_ALLOW_LOOPBACK=1`；这些只用于本次隔离测试，不算无需配置的打包体验验收。
- 所有来源适配器尚未连接到配对传输，接口明确返回 `data_transfer_available=false`，页面显示开发状态，**不会把配对成功显示为内容已同步**。好友权限、模型权限与个人内容没有因配对自动复制。

自动化证据：初始协议 21 项通过，接口/共享后台任务/重装路由/Owner 守卫/HTTP 非阻塞与无地址控制补充后 37 项通过。前端全量 **145 passed / 31 files，22.13 秒**，包含默认范围为空、身份未确认禁用批准、过时预览丢弃、剪贴板失败、批准失败保持待确认、移除审阅、策略修订和暂停失败。TypeScript、Ruff、生产构建通过；最终主 JS `index-BVQ-We6Z.js` 为 586.70 kB，仍有既存的 500 kB 包体提示。

首次后端全量 **1216 passed / 1 failed / 29 skipped / 5 warnings，222.41 秒**；失败为新增后台任务未加入现有任务名称清单，补齐后该用例 **1 passed，7.16 秒**。随后补充加密存储校验、授权与移除的串行性、未来/损坏文件保留；最终全量结果另补于下文，前述结果不冒充修改后的全量通过。

浏览器证据：`scripts/accept_device_pairing_node.py` 分别启动两个独立合成节点 A/B，端口 18873/18874，公开发现与模型关闭。实际页面完成 A 仅选择收藏并创建邀请 → B 粘贴并审核身份、选择收藏、明确确认归属 → A 显示同一核对码并明确批准 → B 确认回执 → 两端显示配对完成且只允许收藏。B 暂停后 A 显示对端暂停；停止 B 进程、使用原目录 `--reuse` 重启，配对和暂停状态仍在；恢复后 B 移除 A，先显示本地已移除待通知，再重试通知，A/B 都显示已移除。未使用内部写文件伪造配对。

浏览器检查发现全局输入框宽度挤压复选框文字，已在设备页限定复选框尺寸并重新构建、刷新后复查。视口 1280 像素，页面宽 1266，未发现越出视口的内容；其他尺寸未在本轮实测。测试标签页与节点进程已关闭，隔离目录保留以供复现。浏览器未执行故障注入或旧报文重放，这些只有自动化证据。

待完成：将已授权的加密数据包接入来源桥接与确认，显示真实待处理数、最后成功时间、失败与冲突；范围变化和重新配对必须重置正确的确认状态；继续阅读/搜索与位置选择；内容传输中途移除的端到端验证；数据导出/完整擦除与恢复项处置；无需手工网络配置的实际打包平台和跨网验证。SYNC01、SYNC10、SYNC11 仅推进到开发中，所有通用发布门槛继续待验收。

最终后端全量：**1221 passed / 29 skipped / 5 warnings，235.41 秒**，包含本轮全部配对、接口、存储校验和授权锁测试。警告为既有 TestClient/FastAPI 弃用提示，跳过项不算通过。验收清单校验为 **95 项：84 in_progress / 11 pending / 0 passed**，没有以测试数量替代产品验收。

## 加密内容传输与来源确认进度

在以上配对实现后，新增 `transfer.py`，将三类已审核数据接到实际来源桥接、HTTP 接口和共享后台工作器：

- 每批只传一种已由双方允许的范围，最多 100 条、12 MiB 明文，签名加密后报文上限 18 MiB。原来的配对/权限控制仍为 64 KiB。传输密钥域同时绑定新配对身份和该配对能力秘密；数据和确认使用不同通道，检查发送者、接收者、范围及双方权限修订。
- 接收先验证整批结构和范围，再在配对锁内启用已同意的来源、提交来源及副本，之后返回加密签名确认。确认绑定确切请求和其中每条操作修订，不能把其他批次或少量伪造确认当成本批完成。模型文件、正文下载、未发送草稿、运行中计算和好友/模型权限不随此传输。
- 来源保留可靠发件记录，掉线或响应丢失后可重新生成批次，不依赖内存中的网络任务。重试以原操作身份合并；发送中途又发生本地修改时，旧确认不能把新值标为完成。会话继续保持原 Provider，收到历史不会启动模型。
- 确认记录按配对身份隔离。任一端暂停/恢复或调整范围后，重新确认该权限版本的所选范围；重新配对也不能借用旧确认。删除旧配对的确认记录不删除来源内容。已移除设备不能提交新批次或旧回执；新配对不受旧移除报文影响。
- 配对、来源、副本按固定顺序加锁；网络等待不持有文件锁。来源提交期间权限不可切换。发送前再次检查当前权限；已在传输中的正文无法通过本机取消收回，移除确认框已说明这种情况。
- 设备页面从当前来源计算待确认数，显示全部所选范围的最后确认时间、等待连接、暂停、存储失败和冲突。文案只说明本机当前变化已获对端确认，不把它等同于对端在线或对端没有未知新变化。各范围单独保存失败状态，其他范围的接收不会擦掉未确认发送错误。
- 收到的阅读位置不伪造本机打开次数或时间；新增派生标记把它纳入阅读历史搜索。未知时间显示“Date unavailable”，不显示 1970 年。正文不随收藏/位置复制，接收端仍明确提示只有元数据，需要另外获取正文。阅读位置冲突保留候选，选择入口仍待补。

自动化验证：传输专项初次为 9 通过 / 1 失败，失败来自合成消息 ID 使用空格，与消息 ID 规则不符；修正测试数据后，来源、传输及接口相关 **32 passed / 1 warning，10.02 秒**。增加真实 HTTP 路由工作器测试后，配对路由与传输专项 **26 passed / 1 warning，4.71 秒**，其中大于配对报文上限的会话正文通过独立数据接口送达。

覆盖三类来源初始合并、仅选收藏不读取/启用会话来源、响应丢失与重启、确认前本机再修改、来源或副本写入失败、暂停/恢复后旧包与旧确认拒绝、网络等待时移除、重新配对重新发送、请求不匹配及伪造确认、双方范围交集、并发会话分支。后端全量 **1232 passed / 29 skipped / 5 warnings，230.56 秒**。前端全量 **147 passed / 31 files，22.28 秒**，包括待确认/已确认/冲突文案及权限界面。该全量运行早于浏览器发现的阅读搜索与日期修正；修正后五个相关后端模块 **58 passed / 1 warning，10.92 秒**，两个前端模块 **17 passed，6.94 秒**，类型检查、Ruff 和构建通过。最终 JS 为 `index-_eSc3VJ8.js`（587.83 kB），保留既有 500 kB 包体提示。

真实进程与浏览器验证：在新的隔离目录启动节点 A/B（端口 18875/18876），A 使用脚本 `--seed` 通过来源 API 保存合成收藏、65% 阅读位置及一段已完成会话，B 初始无这些数据。浏览器从双方真实页面创建邀请、分别选择三类范围、核对身份并批准。后台用实际 HTTP 加密批次传输后，A 显示当前变化已获确认、待确认数 0；B 的 Ask Ryn 出现会话，并显示原接收者、原服务不可用及可读正文，没有静默切换服务或调用模型。

B 的统一搜索找到同步文章及会话；页面实测发现未知时间被展示为 1970 年、远端位置没有归入阅读历史，已修复。停止 B 进程后使用原目录 `--reuse` 重启，搜索显示“Saved · Reading history”和“Date unavailable”，会话仍在；只有元数据时明确显示本地正文不可用。通过 Settings → My devices 返回设备页，两端最终待确认数均为 0，显示确认时间。65% 位置值由来源/HTTP 自动化验证，本轮浏览器没有正文可供验证滚动位置恢复。视口 1280，页面宽 1266，未发现越界元素；标签页与节点已关闭，隔离目录保留。

限制：上述真实双进程在同一 Windows 主机回环网络，不是跨 NAT、两台物理电脑或打包桌面验收。尚未测量实际来源全链路的双端 1 万条基线性能；空批次用于确认连通与范围，后续需评估长会话、多个设备和大规模首次同步。完整个人数据导出/擦除、阅读冲突选择、同步阅读的继续阅读入口、无正文引用处理、网络地址变化与打包跨网路径仍须完成。清单保持 84 开发中、11 待执行、0 已验收。

最终前端全量 **148 passed / 31 files，19.77 秒**，包含上述日期修正。另补成对设备发送有效签名但未获允许的范围检查，要求在接收端来源启用之前拒绝；最终传输专项 **11 passed / 1 warning，8.82 秒**。

## 阅读与收藏冲突的明确选择

“我的设备”新增阅读变更审核区。并发候选显示位置、完成状态、内容版本和设备标识；收藏候选显示保留或取消。没有默认选项，不按最大百分比或设备时间自动决定。设备卡的冲突提示可以定位到该区，会话分支仍从 Ask Ryn 处理。

- Owner 接口读取真实来源候选，提交只接受候选身份及已审核修订，位置值取自来源保存的候选。审核后来源又发生变化时返回修订冲突，不覆盖新数据。
- 选择以一个新的因果操作观察并解决全部已审核候选，在来源的同一次原子写入中保存。提交后的响应丢失可以用同一选择重试；来源又变化后，旧成功请求也不能当作当前成功。
- 页面先报告本机选择已保存，再由设备状态报告对端确认，不提前宣称同步完成。选择删除阅读位置不会删除收藏，也不会伪造本机打开次数或时间；选择更早进度保留对应内容版本。
- 解决记录只作本机幂等元数据，不进入同步值。已授权的正常加密批次传播新选择；旧包重新投递不能让已经解决的候选复活。

自动化证据：来源选择新增 7 项，包括更早位置、内容版本、写入失败、重启重试、过期选择、删除与收藏范围隔离，以及实际加密传输后旧包重放。与来源、路由和传输相关回归 **55 passed / 1 warning，7.34 秒**。前端审核区与设备页 **13 passed，3.82 秒**，覆盖无默认选择、请求失败后相同选择重试、新修订清空旧选择、来源字符串作为文本渲染和加载恢复。

本轮最终全量：后端 **1244 passed / 29 skipped / 5 warnings，229.99 秒**；前端 **153 passed / 32 files，24.17 秒**。TypeScript、Ruff、生产构建通过；主 JS `index-8eyZhq1y.js` 为 591.79 kB，仍有原有 500 kB 包体提示。跳过项和弃用警告不作为验收通过证据。

真实进程与页面证据：复用上一阶段已经通过双端页面配对的合成节点 C/D，端口 18875/18876。两端先停止，再分别通过脚本 `--reuse --position 0.2` 和 `--reuse --position 0.8` 在联网前执行正常来源进度操作，形成并发修改。A 页面显示 80% 和 20% 两个候选，默认均未选择、提交按钮禁用。实际点击冲突定位链接、选择 20% 并提交，页面先显示本机保存及 1 项待确认，随后两端均显示 0 项待确认、没有阅读冲突。只读检查两端来源均为 20%、收藏仍在、打开次数仍为 0。

停止接收端进程后，以原目录 `--reuse` 重启，不再传入位置参数。重新加载 B 页面后，配对、范围、确认状态和无冲突状态保留；只读来源再次确认 20% 和收藏仍在。临时标签页与节点进程已关闭，合成目录保留用于复现。该浏览器路径没有修改系统时钟、注入磁盘故障或加载文章正文，因此不声称已通过时钟偏差、浏览器滚动续读或物理双设备验收。

当前仍需完成：同步位置的继续阅读入口与缺失正文引用处理、实际来源双端一万条基线性能、恢复项关闭/丢弃及完整个人数据导出/擦除、地址变化、原生模型安装与打包跨网路径。以上进展不替代所有功能的通用发布门槛；清单仍为 **84 开发中、11 待执行、0 已验收**。

## 继续阅读与正文版本复核

新增 `/reading`“我的阅读”页面，从导航和首页“Continue reading”进入。提供继续阅读、已保存、阅读历史三种列表，纳入没有本机打开时间的同步位置，不伪造日期或把位置自动归因于某台远端设备。每次最多展示 20 项，可继续展开；读取失败保留已有列表并提供刷新。页面说明正文不随位置同步，连接离线副本和设备管理入口。

- 历史入口及推荐页中已有同步位置的文章使用共同 `ContentViewer`。普通正文加载优先采用本机离线副本；私有文件缺失时明确显示引用和好友下载入口，不自动获取文件权限或宣称已有副本。
- 阅读器在正文可用后读取当前来源修订。已有冲突时可阅读正文，但不选位置、不保存新的进度，提供审核入口。没有冲突但正文版本未知或不一致时，用户可明确采用原百分比或从开头阅读；不默认为准确的段落定位。
- 对同步位置记录规范化可见文本的 SHA-256 版本提示。该值只用来判断进度能否直接复用，不是内容可信度或来源真实性证明；不同布局的百分比也不等于精确段落锚点。
- 每次同步进度写入携带已读取修订，后续写入使用来源返回的新修订。远端更新或删除后，旧阅读器保存被拒绝，页面提供重新读取当前位置的操作。主动重新加载先等待已排队写入结束，避免把保存之前的快照重新作为当前修订。
- 打开已有同步位置只增加本机阅读记录，不新增同步进度操作；无滚动地关闭也不制造位置变更。首次没有位置的打开仍能建立初始阅读记录。真实并发打开的本机计数与并发进度操作分别验证，未为了减少同步操作而丢掉本机历史。

验证：相关后端模块最初暴露两条旧测试仍将每次打开计为新的位置操作；按照上述语义调整后，新增并发进度检查、打开冲突保持候选及旧阅读器拒绝覆盖的测试。专项 **56 passed / 1 warning，5.62 秒**；最终后端全量 **1246 passed / 29 skipped / 5 warnings，242.57 秒**。前端最终全量 **168 passed / 35 files，25.71 秒**，包括列表分类/失败恢复/展开、两个推荐入口、缺失私有引用、正文版本选择、冲突只读、顺序修订及在途写入后重新加载。TypeScript、Ruff 和构建通过；最终主 JS `index-qtsUxUNt.js` 为 597.98 kB，保留既有 500 kB 包体提示。

实际双进程页面证据：复用 C/D 的既有配对，分别以 `--reuse --body` 启动 18875/18876。脚本通过现有 ReaderCache API 在两端各自准备相同合成长正文，正文没有经设备同步传输，也没有访问外部源。A 从新列表打开原 20% 位置，页面因旧位置没有正文版本而停在开头；明确选择原百分比后恢复 20%。键盘 PageDown 产生实际滚动，再关闭保存。A 的可滚动范围 6895 像素、位置 1798 像素，来源按既有四位小数精度保存为 0.2608，并附正文版本。

B 从新列表打开同文，自动恢复 1798 像素，不再出现版本复核提示。关闭后停止 B 进程，使用原目录 `--reuse` 重启，不再准备正文或修改位置；再次从列表打开仍为 1798 / 6895。两端来源的位置、文本版本和阅读修订相同，B 的本机打开次数增加而同步阅读修订不变。最终构建再次刷新并打开，浏览器实际载入 `index-qtsUxUNt.js`，仍恢复相同位置且无错误提示。测试标签页和节点已关闭，合成数据保留。

视觉检查使用 1280 像素浏览器视口，正文、固定头尾和操作按钮可见；观察到外层 1 像素的滚动宽度差（clientWidth 1265、scrollWidth 1266），没有超出视口的内容元素。该轮未覆盖其他屏幕尺寸、不同正文排版或浏览器中的正文更换；版本差异、排队请求和错误恢复主要由页面测试验证。真实公网、跨 NAT、物理双电脑、发布包和系统时钟偏差仍没有本轮证据。

剩余重点：实际来源双端一万条规模性能、完整导出/擦除与恢复项处置、地址变化及无需手动配置的配对、原生模型安装、打包平台及跨网路径。不会以此阶段结果代替完整八项需求验收，清单仍为 **84 开发中、11 待执行、0 已验收**。

## 来源规模测量与重复工作削减（测量进行中）

新增 `scripts/accept_device_sync_scale.py`。两个独立目录分别建立真实节点密钥、阅读来源、加密副本和设备配对；两个路由应用在同一 Python 进程的独立服务线程中通过回环 TCP 通信，保留生产使用的五秒网络超时、报文上限、频率限制和持久化确认。Owner HTTP 接口关闭，脚本通过内部 Owner 服务方法完成合成配对；没有模型或公开发现。

基线在 A/B 各准备互不重复的 5000 条合成旧收藏，通过真实来源迁移后初次同步至各有一万条。只在准备旧数据时直接写合成旧格式文件；没有预置副本或确认记录。初次合并单独计时。随后 A 对其中 100 条逐条执行普通取消收藏操作，再完成双向来源确认；计时包含这些来源写入和最终状态校验，记录加密 JSON 请求/响应字节数（不含 HTTP/TCP 头）。这仍是路由包和来源测量，排除了工作器调度等待、其他节点功能、UI、物理双电脑和跨 NAT。

初步结果：200 条基线、10 条变化的冒烟运行完整结束，来源写入与传输总计 **3.452 秒**，见 [原始结果](scale-smoke-200.json)。一万条基线首次运行在初次合并阶段发生 `PeerTransportError`，尚未进入新增变化计时，见 [原始失败结果](scale-initial-before.json)。该运行期间另做了来源 CPU 分析，有并行负载，不能将其耗时作为隔离环境的对照。来源分析显示，一次修改原先会两次计算全部条目的展示字段，重复校验和缩进 JSON 序列化也占用明显时间。

已完成以下改动，保留文件格式语义、来源身份、因果合并和先持久化后确认的边界：

- 本地编辑只计算当前条目；接收只计算本批条目；冲突选择只计算该条目。未修改的因果实体仍从磁盘加载并检查，不因最近阅读列表淘汰而丢失。全列表读取仍构造完整列表。
- 同步来源写入使用无缩进 JSON，继续使用原子写入和原有容量上限。旧数据迁移备份保持原始文件，不改来源版本或恢复含义。
- 身份核对与来源快照在同一次读取中完成；与已存记录的规范 JSON 相同的重复记录不重新因果合并。比较不使用 Python 对 `0`、`0.0`、`False` 的宽松相等规则。
- 来源和副本实例缓存有界的“范围、条目标识、规范内容指纹”，不缓存记录值。每次仍读取文件，副本仍解密并检查版本、身份和结构；内容变化后重新验证。没有使用文件时间戳判定内容没变，没有放宽网络输入的验证。来源最多 40000 个验证指纹，副本最多 30000 个，达到上限清空后重新验证。
- 元数据控制字符检查改为等价的 ASCII 控制字符匹配，减少逐字符 Python 调用。

验证：相关来源/桥接/记录/副本/传输测试最终 **77 passed / 1 warning，11.76 秒**，Ruff 通过。新增覆盖未编辑的远端收藏和位置在历史淘汰后仍完整、原文件中的无效候选不能借宽松数值相等跳过验证、重复操作更改数值编码仍拒绝、验证指纹绑定标识和范围且有容量上限。此前全量 **1249 passed / 29 skipped / 5 warnings，225.52 秒** 早于指纹缓存的后补变更，不能代替后补的全量结果。

一轮中间测量在完成 A 的 31 批发送后，为应用后续接收端优化而明确停止；它没有得出验收结论。最新版本的一万条测量已重新启动，之后自动执行后端全量回归，目前尚无最终结果。**SYNC04 仍在进行，60 秒目标未获证明**；最终结果必须继续补录，不以小规模、来源片段或前一次全量测试冒充完成。

## 已删除恢复副本的再次保留

在 `093f683` 上补齐一个恢复入口：原分支曾被保留为独立会话，用户又删除该副本后，原按钮明确展示“再保留一份”的审核区。只有用户选择后才使用另一个会话身份；取消不写入。确认区获得焦点，取消后回到原分支按钮。

新身份由已审核的来源、修订、候选及被删除副本身份确定。断线、页面重开或节点重启后重试仍查询同一身份；若该副本也被删除，需要再次明确选择。节点检查前一副本确实已删除且属于同一恢复意图，拒绝替换仍存在的副本、错配身份或过期审核。副本及恢复意图在同一次原子写入中保存，保留原 Provider、服务键及网络，不发起推理。

验证：会话来源和 Owner API 专项 **21 passed / 1 warning，2.88 秒**；Ruff 通过。前端全量 **172 passed / 35 files，28.12 秒** 早于最后的焦点修正；修正后相关两文件 **12 passed，3.89 秒**，TypeScript 与生产构建通过。最终 JS `index-BdkpYEtQ.js` 为 599.26 kB，仍有原有 500 kB 包体提示。后端全量已排在规模测量之后，当前未取得结果。

真实页面步骤见 [恢复副本浏览器记录](recovery-replacement-browser.json)：合成分支保留、删除、取消、键盘确认、节点停止重启以及重复确认均实际执行。重试打开同一副本，原模型不可用时仍能阅读。浏览器实测不是双物理设备、打包桌面或跨网验收。完整恢复项丢弃和个人数据擦除尚未交付，SYNC08 保持开发中。

测量环境补充：本节的专项测试、前端全量/构建和隔离浏览器节点与万条测量的首次合并阶段短暂重叠。因此该轮首次合并耗时不能视为独占机器的对照结果；浏览器节点在增量写入阶段开始前已关闭。万条测量进程自身未改代码、未重启，后续仍需记录最终结果与限制。

## 明确丢弃待恢复内容

在 `e0faa89` 上新增 Owner 丢弃接口与 Ask Ryn 审核区。仅限已删除会话；仍在普通历史中的会话必须先从历史删除，原任务还在核对时拒绝丢弃。审核令牌同时绑定同步修订及本机未发送草稿，新增内容或草稿变化后旧审核失效。它只用于防止过期操作，接口仍先检查 Owner 权限。

确认时原子保存删除决定和重试记录，清除该会话的恢复正文与本机草稿，并复用已有因果删除传播。旧消息不能重建原会话。独立保留的会话不被删除；订单结果、旧浏览器副本和备份不在该操作范围中，界面明确解释。成功后仅报告本机保存，远端状态由设备页确认。审核、取消和成功反馈均有键盘焦点；没有确认的响应仍允许同一决定重试。

来源与 API 最终 **24 passed / 1 warning，3.63 秒**，Ruff 通过。包含真实任务服务尚未结束时拒绝丢弃、草稿变化拒绝旧审核、源文件写入失败保持原样、重启幂等、另一来源应用删除后拒绝旧包及独立副本保留。前端全量 **176 passed / 35 files，30.68 秒**，TypeScript 与生产构建通过；JS `index-CyA-zBf1.js` 为 601.67 kB，仍有 500 kB 包体提示。后端全量仍排在万条测量结束之后，本节尚不能给出全量通过结论。

[真实页面记录](recovery-discard-browser.json) 使用上一节的隔离合成节点：审核、取消、确认后恢复项消失；停止节点后重启，原恢复项仍消失，另一个活跃会话的分支与独立保留副本均在。测试页面已关闭，节点已停止。本节测试/构建和浏览器也与万条首次合并阶段重叠，并在增量阶段前结束。完整个人数据擦除、规模目标及打包跨网验收仍未完成；没有把 SYNC08 标为通过。

## 万条规模最终测量：未达到 60 秒目标

上述持续运行的测量已正常结束，完整结果见 [scale-10000-result.json](scale-10000-result.json)。测量进程加载的是 `093f683` 的同步/阅读实现；期间的两个会话恢复提交不改变该进程中的收藏实现。

| 阶段 | 实测结果 |
|---|---|
| 两个不相交来源迁移 | 2.496 秒 |
| 双端合并为各 10000 条来源记录 | 1866.666 秒，200 批、20000 个确认 |
| 100 次正常来源变更写入 | 63.864 秒 |
| 变更传输及最终双端确认 | 35.819 秒，2 批、200 个确认 |
| 增量合计 | **99.682 秒，未达到 60 秒目标** |
| 增量加密 JSON 字节数 | 164740，含请求和响应，不含 HTTP 头 |

两端来源均保留 10000 个因果实体，100 个目标收藏均为取消状态，最终所选范围获得双方确认；脚本的完整性断言通过。首次合并阶段与本轮其他测试/页面工作有重叠，不能声称独占机器的基准对比。增量阶段前测试浏览器节点已关闭，前端测试结束；该测量仍只是同机两个 TCP 路由服务（同一 Python 进程的独立线程），不包含调度等待、完整节点工作负载、打包桌面或跨网延迟。

测量之后对来源副本进行三次写入剖析，见 [安全摘要](source-profile-summary.json)。主要工作仍是完整文档读取/校验、重复计算实体键与内容指纹，以及 JSON 序列化和持久化。原子落盘仍需保留，不能用跳过持久化换取达标。该剖析和后端全量回归有重叠且带分析器开销，只用来定位下一步工作，不作为耗时验收。

最终后端全量已完成：**1257 passed / 29 skipped / 5 warnings，228.85 秒**。本次测试在规模测量结束后启动，包含 `33ef6d8` 的全部代码（两项会话恢复增量及前述验证缓存），期间没有修改产品代码。跳过项及已有弃用警告不算产品通过证据。配合最终前端 176 项、TypeScript、Ruff 和构建结果，只确认该候选的自动回归；验收清单仍为 84 项开发中、11 项未执行、0 项完整通过。

## 减少重复持久化与读取后的增量复测

在 `19fcb07` 上继续优化，存储与事务说明见 [source-v3-storage.md](source-v3-storage.md)。本次合并来源导入和待同步列表/回执事务；同一状态快照同时计算待处理与冲突数量。已授权的范围检查与来源读取合并。实体键哈希最多缓存 30000 项；规范 JSON 只复用编码选项，字节格式不变。

阅读来源升级为 `ryn.consumption.v3`：已经一致的当前值和已应用值不重复保存；不同的并发位置仍是完整独立对象。旧 v2 首次写入前备份，旧格式仅阅读不重写。实际万条基线的两端 `.v2.migrated` 与原文件 SHA256 均一致。A 文件从 10756640 字节变为 6018149 字节，B 从 12300989 字节变为 7452997 字节；同步记录、元数据总量与确认检查保持不变。

以下均为复制原来已通过真实 TCP 同步的合成目录后，核对持久化配对/回执并执行的**增量复测**；没有制造回执，也没有重新测量首次合并。动作从已取消的 100 个收藏重新收藏，计时包含正常来源保存和首次 v2→v3 写入备份（适用的版本）。前端测试、其他验收节点和剖析器未与增量计时同时运行。

| 版本步骤 | 来源写入 | 传输与最终确认 | 合计 | 60 秒目标 |
|---|---:|---:|---:|---|
| 合并副本事务和状态读取 | 60.598 秒 | 24.195 秒 | [84.794 秒](scale-combined-84s.json) | 未达到 |
| v3 共享一致投影 | 44.401 秒 | 21.997 秒 | [66.398 秒](scale-compact-66s.json) | 未达到 |
| 合并范围启用读取，压缩 JSON 空白 | 44.142 秒 | 18.123 秒 | [62.265 秒](scale-compact-62s.json) | 未达到 |
| 复用规范编码选项，存储不再排序全部键 | 41.996 秒 | 17.140 秒 | [59.137 秒](scale-compact-59s.json) | 本轮达到 |

最终增量均为 2 批、200 个双方确认、164476 个加密 JSON 字节（不含 HTTP 头）。两端各保留 10000 个因果实体，100 个目标变更均生效。59.137 秒的余量较小，仍须执行新版全新目录的初始合并及增量、完整节点与打包/跨网验收；SYNC04 不标记完整通过。

复用基线的首次尝试在计时前因脚本断言失败，保留 [失败记录](scale-reused-baseline-check-failure.json)。原因是脚本把已取消收藏的普通阅读列表数量也要求等于全部因果实体数量；修正后分别验证实体总量、实际收藏集合和双端规范记录相同，没有删掉源数据完整性断言。

检查记录：事务、来源、会话、传输与键缓存相关 **99 passed / 1 warning，18.34 秒**；增加 v3 后读取/迁移/传输专项 **59 passed，7.55 秒**；最后编码兼容及相关存储/传输 **107 passed / 1 warning，7.18 秒**，Ruff 通过。后端全量已在最终增量测量后启动，结果尚待补录。前端没有代码变化，上一轮 176 项结果仍只对应其原有前端版本。

最终全量结果：**1271 passed / 29 skipped / 5 warnings，227.64 秒**，包含本节最终版本的全部后端代码，运行期间没有修改产品代码。Ruff 与差异空白检查通过。新版完整的全新目录测量仍待完成，因此保留上述复用基线的限制，不把 59.137 秒扩大解释为物理双设备、打包桌面或跨网表现。

## 会话来源擦除

`5198d80` 增加内部的审核与原子擦除步骤，见[数据范围与验证](personal-data-maintenance.md)。已覆盖普通会话、本机草稿、冲突/恢复项及独立恢复副本；保留删除标记和任务身份，避免旧数据或重试重新产生正文和重复请求。相关 **61 passed / 1 warning，11.01 秒**，Ruff 通过。

该步骤尚未接入设置页。迁移备份、同步副本、搜索索引、订单结果与旧浏览器数据需要后续协调清理；完整导出、界面及远端确认验收仍未完成。验收状态保持不变。

## v3 全新目录规模测量

在 `c8cec7a` 提交的代码上启动新版完整测量，使用新的合成目录，两端各有 5000 条互不重叠的旧格式收藏，经真实来源迁移、配对和 TCP 传输后各自收敛到 10000 个因果实体。没有复用旧目录、构造回执或跳过首次合并。原始结果为 [scale-v3-fresh-result.json](scale-v3-fresh-result.json)。

| 项目 | 结果 |
|---|---:|
| 两端来源迁移 | 2.254 秒 |
| 首次合并及最终确认 | 900.479 秒，200 批、20000 个确认 |
| 100 次正常取消收藏写入 | 38.966 秒 |
| 增量传输及最终确认 | 16.396 秒，2 批、200 个确认 |
| 增量合计 | **55.361 秒，达到 60 秒参考目标** |
| 增量加密 JSON 字节数 | 164740，包含请求和响应，不含 HTTP 头 |

测量终态为 `measured`，脚本验证两端各保留 10000 个因果实体、100 个目标值均正确。原始结果与仓库内证据文件逐字节一致。首次合并期间有本轮来源擦除测试和代码/文档编辑；增量计时期间没有并行运行这些测试、剖析器或验收节点。新增的 `ask_ryn/privacy.py` 尚未接入运行代码，不参与本次测量。

环境仍为 Windows 11、Python 3.12.10，同机同一 Python 进程内两个独立线程的真实 TCP 路由服务及独立存储，未包含调度等待、完整节点后台工作负载、打包桌面、物理双设备或跨网延迟。不能把两次不同运行中的数值差异当作严格独占环境的性能对比。SYNC04 继续保留这些未完成的验收边界。

本轮新增来源擦除提交后的后端全量回归在此测量结束后执行：**1287 passed / 29 skipped / 5 warnings，225.52 秒**。运行期间产品代码保持不变；跳过项和已有弃用警告不算需求通过。前端代码没有变化，本轮没有重复执行前端测试。验收清单仍为 84 项开发中、11 项待执行、0 项完整通过。

## 节点会话清理协调

在来源事务之后接入 Owner 清理接口，加密保存来源、副本、已审核备份、搜索重建和订单结果的分步进度。支持响应丢失/重启后继续、重新审核变动备份、取消尚未提交的操作。索引写入和来源锁顺序经过协调，旧原子临时副本也列入审核；用户后来创建的内容不随旧操作重试被擦除。详情与边界见[数据维护记录](personal-data-maintenance.md)。

最终后端全量 **1308 passed / 29 skipped / 5 warnings，228.29 秒**；Ruff 与差异空白检查通过，运行期间没有修改产品代码。测试包含完整节点的真实清理依赖，以及保留订单身份、状态和余额的消费结果清理。前端无改动，设置页、浏览器旧副本、完整导出、其他数据范围和远端操作仍未完成，验收状态不提升为通过。

## 设置页与浏览器清理实测

设置页已接入审核、确认、持久化进度与失败恢复；旧浏览器数据库的清理单独确认。浏览器存储 v2 在同一事务内清除副本并保存删除标记，防止旧写入恢复；原 v1 密文和密钥在升级时保持。实际隔离节点完成取消、键盘确认、节点重启、浏览器重载、再次导入无恢复，见 [cleanup-settings-browser.json](cleanup-settings-browser.json) 及[实现边界](personal-data-maintenance.md)。

最终前端 **191 passed / 38 files，25.12 秒**，TypeScript、生产构建、验收脚本 Ruff 与差异空白检查通过。JS 为 `index-CTCUJ2r0.js`（615.79 kB），CSS 为 `index-GUdLW4ng.css`；已有 500 kB 包体警告仍在。浏览器观察的各版本与自动测试最终版本分别记录，最后增加的加密前删除检查由自动测试覆盖。后端产品代码未改变，沿用 `567c5f7` 的回归证据。

完整导出、其他数据范围、操作记录容量处理、远端确认及打包/跨网验收仍未完成；不把这一会话清理流程当作全部数据维护或产品验收通过。

## 产品导出与清理历史轮换

`69d797b` 修复累计 32 次清理后无法继续的问题：终态记录轮换，未完成计划和删除标记保留，操作编号不重用。随后新增 Owner 按范围 ZIP 导出、原文件/离线图片校验、排除清单及设置页恢复操作；见[数据维护记录](personal-data-maintenance.md)、[HTTP 完整性检查](product-export-http.json)和[浏览器检查](product-export-browser.json)。

最终后端 **1333 passed / 29 skipped / 5 warnings**，前端 **198 passed / 40 files**，TypeScript、构建、Ruff 通过。浏览器实测后修复复选框布局并重新构建，最终 JS `index-DiKyx6M3.js`。范围导出和清理历史改进不代表全部擦除范围、完整账户备份或打包/远端验收已经完成。
