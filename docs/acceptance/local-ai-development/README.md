# 本地 AI 开发验证

日期：2026-09-11。分支：`codex/user-product-development`。需求 04 尚未完整验收。

2026-09-12 增量：修复运行时安装中断后的错误复用，测试和待执行桌面步骤见[安装恢复记录](runtime-install-recovery.md)。

同日增量：修复模型下载截断及无效续传响应导致进度丢失，真实 HTTP 与新进程恢复证据见[下载恢复记录](model-download-recovery.md)。

同日增量：取消后按真实结果显示恢复状态，并保留可跨重启恢复的旧配置副本，见[配置恢复记录](setup-rollback.md)。

同日实测：真实下载取消、节点中断和 HTTPS 续传已通过 Windows 页面验证，并修复重启后模型选择丢失。完整下载仍在继续，见[真实下载与页面恢复](windows-managed-recovery.md)。

## 已实现增量

- 本机已配置模型进入 Ask Ryn 服务列表，不依赖公开发布和发现网络。状态区分本机就绪与对外发布，停止或模型不可用时按真实健康检查显示未就绪。
- 同节点请求仍使用原任务 ID、加密消息、容量控制、后台归档及取消逻辑，执行直接进入本机 Provider，不进入 HTTP 对端、ICE 或中转；本机计算额度为零，其他 Provider 的计费规则不变。
- 安装页显示目录中固定的模型来源、许可、模型下载大小及估算磁盘/内存。自动推荐在提交时固定为已经展示的具体档位；变更档位需重新勾选安装确认。
- 结算确认检查点不会遮掉已完成任务的传输和用量信息。

## 自动验证

- 后端全量 1031 passed / 29 skipped（169.19 秒）；前端全量 91 passed / 22 files；TypeScript、生产构建、修改范围 Ruff 通过。
- 新增联调用例使用本地 HTTP 模型替身：服务不发布、Registry 抛出离线错误、服务器配置严格 P2P 时，本机 Ask 仍生成一次并归档；额度不扣除，重复确认不重跑，另一签名节点被拒绝。
- 原生模型取消专项以阻塞适配器验证：使用同一 Provider 取消、运行结束前仍标记结果待核对，最终无残留预留额度。
- 前端覆盖本机问答入口、来源/许可/大小展示，以及确认前无安装、自动档位提交时固定到已审核模型。

## Windows 浏览器与原生模型实测

独立临时节点运行于 `127.0.0.1:18846`，模型端口 `18847`；节点 home 独立于用户数据与开发工作树。主机 Windows AMD64、8 个逻辑 CPU、约 32 GiB RAM，检测到 Docker 未运行、原生运行时未安装。

从真实浏览器页面选择 `light` 模型，核对 Qwen2.5-0.5B-Instruct-Q4_K_M、469 MiB 下载、1606 MiB 磁盘及 Apache-2.0 许可后勾选确认。安装任务 `setup_2e26c0ef55804a5d85410e7aab6a885b` 已完成模型校验、llama.cpp b10774 安装、启动和自检，进度达到 100%。未开启服务发布，本机问答完成且花费为零。

实际停止节点及模型子进程后，使用同一 home 重启节点。历史、会话 ID 和 Provider 绑定保留；页面真实显示未就绪，经“Start runtime”恢复，无需重新下载。该流程验证了手动恢复，未验证自动启动。

过程中修复两项页面状态问题：硬件检测使用该节点的运行时目录，已安装模型不再被误报尚待下载；任务列表和单项状态均合并当前状态的检查点，结算确认后仍显示 `local_runtime`。

追问实测发现，轻量模型会把扁平文本中的历史问题当作当前问题。现为明确支持 `chat_messages_v1` 的 Provider 保留系统、用户和助手角色；文章仍作为不可信用户数据提供。输入保持在原加密任务内，格式进入请求指纹；格式改变不能复用原任务。未声明此能力的服务继续使用文本输入。

新会话 `255cbd4e-c47f-40d1-b2d3-0a2fdec49087` 在浏览器中完成“2+3 → 5；把上次结果乘以2 → 10”。两轮归档摘要与重建的多轮输入一致。原会话含多次错误回答，调整格式后仍重复旧答案，其失败记录完整保留；不能用新会话结果推断模型普遍推理能力或旧会话恢复正确。

逐轮任务、原始回答和进程恢复记录见 [实测数据](windows-native-inference.json)。验收进程及其模型子进程已停止，下载文件保留供后续验收使用。

增量验证：当前工作树后端全量 **1045 passed / 29 skipped**（177.12 秒，包含尚未接入路由的权限存储 4 个测试）；多轮角色、消息格式身份冲突、非法输入、运行时目录和任务列表专项通过，Ruff 通过。前端源代码未因这次后端修正改变，生产构建通过；上次全前端为 91 passed。

本机浏览器加原生运行时验证不能替代打包桌面应用验收；其他平台、好友授权与撤销、完整安装中断恢复仍待完成。
