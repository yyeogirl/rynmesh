# Ask 导出、删除与局部日志核对

2026-09-14，Windows 本机生产构建浏览器、节点端真实加密存储；复用既有合成故障会话。运行时保持关闭，本轮没有推理。对应 ASK10，保持 `in_progress`。

## 问题与修复

- 导出未等待草稿保存；立即导出可能取得旧草稿。针对性回归修复前 3 失败 / 7 通过，修复后 10 通过。现在先串行保存当前草稿，再导出；处理中禁用重复操作，保存或导出失败均保留输入并提供重试，反馈获得焦点。
- 实际 GET 验证 Ask 历史、草稿、导出原先均无 Cache-Control。现在 `/api/local/ask/` 响应加 `no-store`，包括未认证拒绝和已删除记录的 404。未声称此前发生过缓存泄漏。
- 下载提示仅确认准备完成和已请求下载，提示用户核对下载文件。

## 实际页面步骤与结果

1. 复用两个已有浏览器会话，输入独立草稿 `Ask export privacy acceptance 20260914 — unsent draft`，立即点击导出。页面成功反馈获得焦点。
2. 默认下载目录原先无目标文件；操作后出现 `C:/Users/子陵/Downloads/ryn-conversations.json`（8,978 字节）。解析后的 JSON 与 owner 导出一致，包括两条会话、最新草稿和空冲突恢复列表。文件 SHA-256 见检查点。
3. 停止本轮节点，页面再次导出：显示未完成、保留草稿，并聚焦失败说明。重启同一数据目录，在同一页面重试：显示已请求下载。
4. 页面打开 `f64fd70a-974b-43f9-a6d1-64cc5eff4468`。确认框说明删除节点会话及其草稿，旧浏览器恢复副本和订单结果独立存在，运算可能继续。确认后列表与导出剩一条，旧 ID 返回 404，独立草稿仍在。
5. 重启后页面仍只显示 `a4dc83fb-4695-407d-a36c-0fbd7ec99a88`，独立草稿不变。六笔 consumer 订单的身份和状态在导出、删除、重启三个检查点完全一致。

## 隐私核对与限制

- 导出响应仅含版本、会话、独立草稿和冲突恢复字段；真实文件与响应一致。此 fixture 无冲突数据，有冲突的数据范围另由现有存储/隐私专项覆盖。
- 节点身份私钥、消息私钥、控制令牌的原始文件内容及 base64/hex 形式未出现在导出响应与本轮捕获日志中；比较只在内存执行，证据不包含凭证。
- 本轮启动、导出、删除、重启日志中未检出这些会话的输入/输出字符串或独立草稿。此结论只适用于所捕获日志，不替代推理期间全链路日志审计。
- 普通删除按确认框范围执行，未声称删除已有下载文件、浏览器恢复副本、独立订单结果或所有同步设备的数据。
- 未验证打包桌面、macOS、跨公网和真实 GPU。ASK10 和通用标准未据此标记全部通过。

## 自动验证

- 前端：44 文件 / 244 测试通过；TypeScript 与生产构建通过，产物 `index-CVweTJRC.js`。保留原有大块体积提示。
- 后端相关历史、隐私、运行、产品导出：63 测试通过；修改的 Python 文件及观察脚本 Ruff 通过。未重复全后端测试。
- 机器证据：[export-privacy-checkpoints-20260914.json](export-privacy-checkpoints-20260914.json)。只读观察脚本：`scripts/accept_ask_export_privacy.py`。
