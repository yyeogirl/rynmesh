# Ask 失败恢复浏览器验收（2026-09-14）

环境：Windows、本机生产节点 HTTP 18950、浏览器；专用合成运行时 HTTP 18951。节点从空目录创建，经产品设置表单配置服务；所有六次 Ask 请求均由浏览器输入、审核接收方后确认发送，没有预置会话或任务结果。

合成服务仅用于可控故障和恢复回答，不作为真实模型、GPU、打包桌面或公网网络验收。

## 流程和结果

| 操作 | 浏览器及节点证据 |
|---|---|
| 新安装，配置合成服务后打开 Ask | 没有伪造历史；选择服务时显示接收方，确认后创建独立会话 |
| 健康检查成功，推理返回 runtime_busy | 显示服务忙碌、等待或换服务，以及“Check original task” |
| 恢复运行时后检查原任务 | 明确原任务失败且没有重新提交；订单和 HTTP 推理次数不变 |
| 输入另一条问题并再次确认 | 新任务成功，显示明确的合成恢复回答；只有此时新增一次推理 |
| /v1/models 返回 model_not_ready | 页面顶部显示本地模型未就绪和模型设置入口；实际提交时发现后台误归为 provider_unavailable |
| 修复后重启节点，同一会话再次提交未就绪请求 | 改为“模型未就绪，检查模型设置或选择其他服务”；没有创建推理订单，没有调用生成接口 |
| 健康检查成功，生成返回 runtime_unavailable | 显示运行时不可用和检查运行时的动作 |
| 真实停止合成运行时进程，节点保持在线，再次页面提交 | 显示 Provider 无法连接其模型运行时，建议检查本地 AI 设置；没有产生推理订单 |
| 点击 Manage local model，恢复运行时并点 Refresh | 正确进入设置页，刷新后变为 ready on this device，发布仍关闭 |
| 从历史继续原会话，检查原断连任务 | 原失败及服务绑定保留，不重新发送 |
| 再停止并重启节点、浏览器重新加载、检查原断连任务 | 六个任务状态和 ID、十二条消息摘要、三笔实际推理订单及五次运行时调用完全不变 |

运行时五次 POST 中，两次是配置阶段的自检/能力探测，三次是 Ask 的忙碌、恢复成功和运行时不可用请求。三个提交前失败（修复前未就绪、修复后未就绪、真实断连）只保存 Ask 失败回执，没有进入推理订单。

会话 ID：`f64fd70a-974b-43f9-a6d1-64cc5eff4468`。节点进程 46916 → 63132（加载修复）→ 23704（恢复复验）；运行时 51948 → 5856。修复前错误记录继续保留，未改写成通过结果。

## 修复与验证

适配器健康结果新增受限的错误码。提交前检查明确区分模型/运行时未就绪和 Provider 不可达；已知失败码传给 Ask，未知健康错误只归为 service_unhealthy。关闭分享但健康的服务仍按 Provider 不可用处理，不冒充模型故障。

- 提交前分类、HTTP 错误传递专项 18 passed。
- Provider、运行时 HTTP、Ask 工作器及上下文相关回归 129 passed / 4 skipped；跳过项不算通过。
- 四个变更 Python 文件 Ruff、git diff --check 通过。
- 四组检查点比较确认“检查原任务”、恢复和节点重启没有改变会话、订单或运行时调用数。
- 前端代码没有变更，没有重复运行前端构建。

复现脚本：[accept_ask_error_browser.py](../../../scripts/accept_ask_error_browser.py)，分别以 `node`、`runtime` 启动，`node --resume` 恢复节点，`observe --label NAME` 记录元数据。故障只通过专用目录 `mode.txt` 控制合成运行时，以及停止该运行时进程制造；没有改写产品响应或跳过权限。

[十个安全检查点](error-browser-checkpoints-20260914.json)只保留任务状态、ID、错误码、消息摘要和调用模式，不保存问题正文或凭证。浏览器可见结果记录在上表。

本轮补充 ASK07 的忙碌、未就绪、运行时不可用/真实断连及恢复流程；P2P 容量不足的浏览器分支、远程 Provider 不可达、平台/公网验收仍未完成，ASK07 及完整 95 条标准不因此宣布通过。
