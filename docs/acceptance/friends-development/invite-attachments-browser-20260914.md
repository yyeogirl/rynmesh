# 并发邀请、自然过期与附件页面验收

2026-09-14，Windows，本机三个独立空节点 A/B/C（18970/18971/18972），生产浏览器构建 `index-BMZYq3oJ.js`。通过正常页面创建、审核、接受和分享，没有预置关系或消息。沿用 `accept_first_sharing_node.py`；本轮没有邮箱服务、安装包安装或跨公网。

## 邀请与恢复

- A 创建一份未使用邀请保留至自然过期；使用原定 15 分钟，没有改变时钟或缩短 TTL。B 最终审核时显示邀请已过期、请索取新邀请。
- A 创建第二份邀请后取消，C 审核并确认接受，被明确告知邀请已取消，没有建立关系。
- B/C 分别审核第三份邀请后，同时点击确认。出站开始时间为 11:10:39.537621 / 11:10:39.536653 UTC，A 的两个响应结束时间为 11:10:39.613233 / 11:10:39.615269。请求确实重叠；只有 A/C 建立同一关系，B 显示已被使用。
- C 再次粘贴同一成功邀请并确认，仍返回原关系，双方关系数、身份均不变。
- A 给 B 创建新邀请，B 成功接受并发送第一条消息，收到送达确认。B 没有 A/C 的旧附件或消息。
- 三个节点重启后，关系及消息保持：A 两个好友/四条消息，B 一个好友/一条消息，C 一个好友/三条消息。各消息身份与投递状态不变；权限仅为消息、小附件和内容卡片。

## 附件

所有文件均为本轮在专用测试目录生成的合成数据。浏览器通过真实文件选择器选取，不注入页面状态或直接调用发送接口。

| 文件 | 发送与接收 | 主动保存核对 |
|---|---|---|
| 71 字节 UTF-8 文本，A→C | 完整接收，A 获接收确认 | 默认下载目录出现文件；SHA-256 与源文件一致 |
| 2,048 字节任意二进制，C→A | 完整接收，C 获接收确认 | 内置浏览器未找到预期落盘文件；常规 Chrome 打开同一节点、同一消息，正常点击一次保存后取得文件，字节与摘要均与源文件一致 |
| 恰好 5 MiB（5,242,880 字节），A→C | 正常发送，A 获接收确认 | 默认下载目录出现文件；SHA-256 与源文件一致 |
| 5 MiB + 1 字节 | 选择后提示超限，Send message 禁用；两端消息仍为零 | 未发送 |

2 KiB 文件在内置浏览器中的保存差异尚未定位：没有可见页面错误或已捕获的 console 错误，Playwright、可访问性点击和重开页面未取得预期文件。随后常规 Chrome 在相同服务、相同消息与未修改的前端上，一次点击即取得 2,048 字节文件，SHA-256 为 `10fc3c51a152e90e5b90319b601d92ccf37290ef53c35ff92507687d8a911a08`，与原文件和接收节点 HTTP 响应一致。保留不同浏览器的结果，不用 Chrome 的成功声称内置浏览器或打包桌面已通过。

浏览器的 download 事件等待曾超时，但同次文本文件实际已落盘；因此本轮最终保存以真实文件存在和摘要为证据，不以事件等待单独判定。没有绕过浏览器安全提示，也没有将二进制改名为文本来获得更容易的通过结果。

## 修复与检查

自然过期验收发现：接收端能正确拒绝，owner 邀请列表接口却仍返回 `status=active`，直到有人实际尝试消费才可能更新。现在列表按照原到期时间投影为 expired，不改持久历史，不改变 used/cancelled，也不改变接受时的安全检查。修复后真实节点返回 used/expired/cancelled/used 四份邀请状态。

新增回归在原实现失败（active != expired），修复后相关好友、路由、配对恢复及投递 **20 项通过**，修改的 Python 及观察脚本 Ruff 通过。前端未改，沿用上轮已验证生产构建，没有重复完整前后端测试。

机器证据：[invite-attachment-checkpoints-20260914.json](invite-attachment-checkpoints-20260914.json)。`observe_sharing_invites.py verify` 核对并发、唯一关系、重复接受、超限、重启与权限；`verify-downloads` 在内置浏览器阶段非零退出，Chrome 对照后，三份文件均存在且摘要匹配，最终通过。证据不保存邀请 URI、关系秘密或附件正文。

SHARE03/04/05 的本机证据增加，正式状态仍为 in_progress。内置浏览器差异、打包桌面、跨公网、GitHub CI 与完整隐私仍不算通过。节点与浏览器验收页已关闭；原文件、三个已下载文件及三个独立数据目录保留。
