# 页面受众调整、离线撤销与发布失败恢复

2026-09-14，候选 `b10e00e`。复用原有 Author/Bob/Carol 三个独立 Windows 节点和真实配对关系，端口 18860/18861/18862，Chrome，无模型。原发布已停止、版本 5；Bob 已取消关注并保留一份独立副本，Carol 仍关注。未重新配对或种入首条更新。

对应 FOLLOW06、FOLLOW08、FOLLOW09、FOLLOW10，补充重启恢复。此次没有产品代码修改。

## 实际页面流程

1. Author 从旧发布的 Edit draft and audience 进入，选择 Bob 和 Carol 并保存。草稿升为版本 6，但发布仍停止、当前受众为空。Bob 从页面主动恢复关注，双方均无可见更新。
2. 使用 Windows 文件句柄阻止原子替换 Author 的加密 feed 状态文件。页面复核并确认发布后得到明确失败提示，草稿及版本 6 不变，没有显示已发布，双方列表为空。
3. 保留文件故障，终止并重启 Author。重新加载页面、从原记录进入编辑，仍能恢复同一草稿和两位受众。释放文件占用后，页面重新复核并发布成功，仍为同一发布 ID，版本 7。
4. Bob、Carol 分别只出现一个版本 7 条目，标记 Unread / Updated。Author 展示两位当前有权访问者，并明确权限不代表送达或阅读；没有虚构阅读名单。
5. 停止 Bob 节点，保留已打开页面。页面显示本地节点无法刷新，保留最后检查时间 **20:41:26** 及旧版本 7，并提示离线期间权限可能变化。
6. Author 页面取消 Bob 的勾选、保存草稿。版本 8 的草稿仅选 Carol，但实际发布仍为版本 7、仍允许两位好友。只有再次复核并确认发布后，版本 9 才实际收窄到 Carol；页面当前受众随之更新。
7. Carol 的同一条目更新为版本 9，未重复生成条目；离线 Bob 仍显示原检查时间和版本 7，没有被冒充为远程清除。
8. 恢复 Bob 节点。页面自动消除连接错误，更新列表清空，关注仍开启。通过 Bob 的 Owner API 使用原好友身份分别请求版本 7 和当前版本 9，均得到 **409 / feed_publication_unavailable**。
9. Author 从页面确认 Stop sharing，版本升为 10；当前受众为空，Carol 列表清空。Bob 从 My reading 打开原保存副本，正文正常。
10. 停止并重启全部三节点，重新加载页面。版本 10 的停止状态保持，两个接收端均无受保护更新；关注与 Bob 的旧已读版本记录保持，独立副本摘要不变，再次打开仍正常。

## 证据与边界

[结构化检查点](audience-recovery-checkpoints-20260914.json)记录发布/草稿版本及受众、订阅、可见行、只读摘要和安全错误码。旧副本 SHA256 为 `85d354453b67ab3e434d01f629686a549dab3c07ebeb934610c84ef77305c8c2`，全过程保持一致。

`scripts/observe_feed_audience_recovery.py` 的观察路径只读本地 Owner API；`denied-fetches` 发起明确应拒绝的旧身份内容请求，没有下载或保存新副本；`verify` 检查失败前后草稿完全一致、草稿/已发布受众分离、稳定发布 ID、撤权后获取拒绝、副本及最终重启不变。原子写入故障沿用专用文件锁辅助工具，只增加该测试目录的受限目标。

验证器、Ruff 和差异检查通过。产品实现未改变，不重复单元、性能或先前配对测试。最终重载后的三个浏览器页面 warning/error 均为空。页面、文件锁与节点在本轮结束时关闭。

限制：本次离线方式为接收节点进程关闭，浏览器保留先前页面；不代表整机断网、应用冷启动时无需节点也能加载，亦非跨公网或打包桌面。未重新测量 60 秒更新时限，未覆盖全部好友的新好友加入、多页修订和全应用隐私/键盘门槛。

后续复用状态：同一发布停止于版本 10，Bob/Carol 均仍关注 Author，Bob 保留原副本，Carol 没有下载副本。须从此状态继续，不按更早记录假定 Bob 未关注。
