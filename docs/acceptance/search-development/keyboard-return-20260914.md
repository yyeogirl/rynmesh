# 搜索键盘分页与返回位置

2026-09-14，基于 `4c36a39` 的候选。复用上一轮 25 条合成归档消息，启动生产应用，移除传输延迟器；没有重新配对、导入数据或测量搜索性能。Windows Chrome，视口内应用主滚动区高 870 像素。

对应 SEARCH03 和 G07 的局部验收。**未据此声称完整键盘、日期/好友筛选或打包桌面验收通过。**

## 发现并修复的问题

从初始页面用 Tab 到达搜索框，输入关键词；用 Tab、End 设置 Conversations 类型、来源和 Most recent 排序；能够穿过日期输入的各个原生焦点段。继续 Tab 到 Load more results 并回车，第一页 20 条成功变为 24 条，但按钮消失后焦点落到 BODY，下一次 Tab 也没有进入新结果。

现在成功分页后，若用户仍停留在分页操作上或因按钮消失失焦，焦点移到第一条新结果的链接。如果等待时用户已转到其他输入框，保留其焦点。旧查询代次检查继续生效。

新增回归先复现预期链接未聚焦、实际聚焦 BODY 的失败。修复后搜索相关 **15 项**检查通过，包括不抢走等待时已移开的焦点。TypeScript 和生产构建通过，原有大于 500 kB 提示保留；新资源 `index-BgiJBxgZ.js`。此次未改后端，未重复后端或整库测试。

## 实际浏览器复验

- 新构建实际加载。建立新的第一页查询，Tab 到分页按钮并回车；24 条结果显示后，焦点链接等于第 21 条结果（第一条新增结果）的链接。
- 直接回车打开该结果，Ask 页面准确聚焦 `ask-message-message-6`，没有模型仍可读归档。
- 浏览器返回后，关键词、Conversations 类型、来源、Most recent 排序及 24 条结果全部保留。
- 打开前、返回后主滚动区 `scrollTop` 均为 **2375**，`scrollHeight` 均为 **3245**；没有把 window 的位置误作应用容器的位置。
- 修复前的独立测量同样从第 24 条消息返回，2375 像素位置恢复正确。修复聚焦问题后仍保持原有返回行为。
- 浏览器 warning/error 记录为空。

操作限制：工具发送 Alt+Left 没有触发 Chrome 的浏览器级返回；改用受支持的浏览器 `back()` 后完成上述位置核对。没有把工具未触发快捷键判作产品缺陷，也没有把该返回步骤记成已通过的真实键盘快捷键。首次新构建重载保留了已加载的 24 条结果，随后使用不同关键词建立新第一页再复验分页，没有把找不到已消失按钮误判为加载失败。

[结构化检查点](keyboard-return-checkpoints-20260914.json)记录焦点身份、计数、位置和范围，不保存正文或查询原文。页面与生产节点在记录结束后关闭。
