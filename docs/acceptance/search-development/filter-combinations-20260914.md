# 日期、好友与类型组合筛选

2026-09-14，候选 `b6a9b3d`，Windows Chrome。复用真实页面配对后保留的 A/B 消息，只有 A 节点启动；B 的端口在开始与结束检查时均未监听。没有新增好友、消息或模型。

对应 SEARCH03，补充 SEARCH07 与 G07 的日期输入证据。此次无产品代码修改。

## 页面结果

- 页面好友选项只包含仍有效的 B，没有已撤销的 C。
- 指定 B、Conversations、2026-09-14 至 2026-09-14、Most recent，命中原有一条消息。消息实际时间为当天 19:20:50，本机时区为 UTC+8。
- 改为 2026-09-15 当天，零条并显示无匹配说明；旧片段消失。
- 改为起始 2026-09-16、结束 2026-09-15，明确提示检查日期与筛选，结果为空。恢复正确区间后提示消失、原消息恢复。
- 同一好友和日期下，Saved 类型不包含这条消息；Friend shares 类型包含这条收到的消息。
- 从结果打开，页面准确聚焦 `friend-message-e1dd15ea76ad445db79973dec056e059`。对端离线仍能读取本机已有消息。
- 浏览器返回后，关键词、B、Friend shares、两个完整日期和 Most recent 均保留，仍命中一条；该短列表滚动位置前后均为 0。长列表的 2375px 恢复沿用[上一轮证据](keyboard-return-20260914.md)，没有重复测量。
- Clear filters and keywords 后，日期/好友/类型与关键词全部为空，排序回到 relevance，显示空搜索说明，不自动显示全部记录。最终浏览器 warning/error 为空。

## 原生日期控件说明

工具最初对 `input type=date` 执行通用 `fill`，没有实际保存值。读取 DOM 发现两个日期仍为空，因此最初的一条结果只算好友/类型证据，不算日期通过。

实际控件显示“日/月/年”。随后通过箭头键定位原生日期段并逐键输入，明确读到 `2026-09-14`，再执行排除、错误与恢复场景。没有设置 DOM 属性或改 React 状态，也没有为适配工具改动产品。

[结构化检查点](filter-combinations-checkpoints-20260914.json)保留日期、类型、数量、焦点和限制，不保存正文、查询原文或凭证。无实现改动，不重复单元测试、性能和索引恢复。此证据不能替代打包桌面、全应用键盘或完整隐私验收。页面与验收节点在本轮结束时关闭。
